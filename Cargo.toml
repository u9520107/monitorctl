[package]
name = "monitorctl-core"
version = "0.1.0"
edition = "2024"
default-run = "monitorctl"

[[bin]]
name = "monitorctl"
path = "src/main.rs"

[[bin]]
name = "monitorctl-tray"
path = "src/tray.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.9"
windows = { version = "0.62.2", features = [
    "Win32_Devices_Display",
    "Win32_Foundation",
    "Win32_Graphics_Gdi",
    "Win32_UI_Input_KeyboardAndMouse",
    "Win32_UI_Shell",
    "Win32_UI_WindowsAndMessaging",
] }
